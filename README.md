# Python Project Management CLI Tool

## Overview
A command-line tool to manage users, projects, and tasks with persistent JSON storage.

## Setup Instructions
1. Install dependencies: `pip install -r requirements.txt`
2. Run the CLI: `python main.py --help`

## Features
- Manage users
- Manage projects
- Manage tasks

## Known Issues
TBD
